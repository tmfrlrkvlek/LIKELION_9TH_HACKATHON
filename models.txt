search

qna
    book -> isbn, title, author, publisher(text)
    room -> book(onetone model)
    qna -> room(model), title, writer(profile model), content, isfile, pubdate, chapter(int), page(int), qnum(text)
    file -> profile, qna(model), order, filename
    comment -> pubdate, content, writer(profile model), parent(int), depth(int), selected(default - false), isfile

account
    user -> password, username
    profile -> point, user(model), nickname

- 프론트 전달사항
    chapter명, page 입력하는 부분에 숫자로만 입력 가능하도록